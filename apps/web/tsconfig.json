{
	"extends": "@business-management/typescript/base.json",
	"compilerOptions": {
		"plugins": [
			{
				"name": "next"
			}
		],
		// I added this line to store tsbuildinfo file in node_modules/.cache to keep project root clean and avoid VCS conflicts.
		"tsBuildInfoFile": "node_modules/.cache/tsbuildinfo.json",
		"paths": {
			"@/*": ["./*"]
		}
	},
	"include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
	"exclude": ["node_modules", ".next"]
}
